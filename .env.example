# PostgreSQL — local dev (use Docker, Neon, Supabase, ou Railway)
DATABASE_URL="postgresql://user:password@localhost:5432/advocacia_clarissa?schema=public"

# Auth.js — gerar com: openssl rand -base64 32
AUTH_SECRET="troque-este-valor-em-producao"

# URL pública do app (Auth.js usa em redirecionamentos)
AUTH_URL="http://localhost:3000"
